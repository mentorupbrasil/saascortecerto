/** Shared storage constants (safe for client import) */
export const MAX_INLINE_BYTES = 500 * 1024;

export const ALLOWED_CONTENT_TYPES = new Set([
  "image/jpeg",
  "image/png",
  "image/webp",
]);
